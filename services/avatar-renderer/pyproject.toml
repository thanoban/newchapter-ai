[project]
name = "newchapter-avatar-renderer"
version = "0.1.0"
description = "Beyond Presence avatar renderer integration for NewChapter"
requires-python = ">=3.11"
dependencies = [
  "livekit-agents>=1.3,<2",
  "livekit-plugins-bey>=0.2,<1",
]
